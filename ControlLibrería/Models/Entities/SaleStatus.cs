namespace ControlLibrería.Models.Entities
{
    public enum SaleStatus
    {
        Active = 0,              // Venta activa
        PartiallyCancelled = 1,  // Parcialmente devuelta
        Cancelled = 2            // Completamente cancelada
    }
}
